pwd
exit
quit
cube <- function(x,n) { x^3 }
cube(3)
x <- 1:10
if (x > 5) { x <- 0 }
x<-0
x
x<- 1:10
f <- function(x) {
g <- function(y) {
y + z
}
z <- 4
x + g(x)
}
z <- 10
f(3)
x <- 5
y <- if(x < 3) {
NA
} else {
10
}
y
h <- function(x, y = NULL, d = 3L) {
z <- cbind(x, d)
if(!is.null(y))
z <- z + y
else
z <- z + f
g <- x + y / z
if(d == 3L)
return(g)
g <- g + 10
g
}
h
h(1)
f <- function(x) {
g <- function(y) {
y + z
}
z <- 4
x + g(x)
}
z<-10
f(3)
rm(ls())
ls()
rm
rm(list=ls())
swirl()
library(swirl)
swirl()
my_sqrt<-sqrt(z-1)
my_sqrt
my_div <- z / my_sqrt
my_div
c(1,2,3,4)+c(0,10)
c(1, 2, 3, 4) + c(0, 10, 100)
(z*2+100)
(z * 2 + 100)
info()
(z * 2 + 1000)
z * 2 + 1000
my_div
directory <- ".\specdata"
directory <- "./specdata"
getwd()
directory <- "D:/Users/d309144/Documents/Coursera/DataScience/specdata/"
id = 1:2
id
id = 1
id
filename = paste(directory,sprintf("%3d.csv",id)
)
filename <- paste(directory,sprintf("%3d.csv",id))
filename
filename <- paste(directory,sprintf("%03d.csv",id))
filename
filename <- paste(directory,sprintf("%03d.csv",id),sep="")
filename
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## NOTE: Do not round the result!
for i in 1:length(id) {
filename <- paste(directory,sprintf("%03d.csv",id[i]),sep="")
data <- open.csv(filename)
}
data
}
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## NOTE: Do not round the result!
for(i in 1:length(id)) {
filename <- paste(directory,sprintf("%03d.csv",id[i]),sep="")
data <- open.csv(filename)
}
data
}
pollutantmean(directory,"sulfate",c(1,2))
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## NOTE: Do not round the result!
for(i in 1:length(id)) {
filename <- paste(directory,sprintf("%03d.csv",id[i]),sep="")
data <- read.csv(filename)
}
data
}
pollutantmean(directory,"sulfate",c(1,2))
head(pollutantmean(directory,"sulfate",c(1,2)))
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## NOTE: Do not round the result!
for(i in 1:length(id)) {
filename <- paste(directory,sprintf("%03d.csv",id[i]),sep="")
data <- read.csv(filename)
data
}
}
head(pollutantmean(directory,"sulfate",c(1,2)))
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## NOTE: Do not round the result!
for(i in 1:length(id)) {
filename <- paste(directory,sprintf("%03d.csv",id[i]),sep="")
data <- read.csv(filename)
data
}
}
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## NOTE: Do not round the result!
n_monitors = length(id);
for(i in 1:n_monitors) {
filename <- paste(directory,sprintf("%03d.csv",id[i]),sep="")
data <- mean(complete.cases(read.csv(filename)[[pollutant]]))
}
}
head(pollutantmean(directory,"sulfate",c(1,2)))
pollutantmean(directory,"sulfate",c(1,2))
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## NOTE: Do not round the result!
n_monitors = length(id);
for(i in 1:n_monitors) {
filename <- paste(directory,sprintf("%03d.csv",id[i]),sep="")
total <- sum(read.csv(filename)[[pollutant]],rm.na=TRUE)
}
total / n_monitors;
}
pollutantmean(directory,"sulfate",c(1,2))
pollutantmean(directory,"sulfat",c(1,2))
directory
pollutantmean(directory,"sulfat",1)
pollutantmean(directory,"sulfsat",1)
pollutantmean(directory,"sulfate",1)
pollutantmean(directory,"sulfate",1)
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## NOTE: Do not round the result!
n_monitors = length(id);
for(i in 1:n_monitors) {
filename <- paste(directory,sprintf("%03d.csv",id[i]),sep="")
total <- sum(read.csv(filename)[[pollutant]],na.rm=TRUE)
}
total / n_monitors;
}
pollutantmean(directory,"sulfate",1)
pollutantmean(directory,"sulfate",c(1,2)
)
pollutantmean(directory,"sulfate",c(1:12)
)
pollutantmean(directory,"sulfate",c(1:1pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## NOTE: Do not round the result!
n_monitors = length(id)
means <- numeric(n_monitors)
for(i in 1:n_monitors) {
filename <- paste(directory,sprintf("%03d.csv",id[i]),sep="")
means[i] <- mean(read.csv(filename)[[pollutant]],na.rm=TRUE)
}
mean(means);
}2))
pollutantmean <- function(directory, pollutant, id = 1:332) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'pollutant' is a character vector of length 1 indicating
## the name of the pollutant for which we will calculate the
## mean; either "sulfate" or "nitrate".
## 'id' is an integer vector indicating the monitor ID numbers
## to be used
## Return the mean of the pollutant across all monitors list
## in the 'id' vector (ignoring NA values)
## NOTE: Do not round the result!
n_monitors <- length(id)
my_means <- numeric(n_monitors)
for(i in 1:n_monitors) {
filename <- paste(directory,sprintf("%03d.csv",id[i]),sep="")
my_means[i] <- mean(read.csv(filename)[[pollutant]],na.rm=TRUE)
}
mean(means);
}
pollutantmean(directory,"sulfate",c(1:12))
pollutantmean(directory,"sulfate",c(1:11))
setwd("~/Coursera/DataScience/ProgrammingAssignment2")
source("cachematrix.R")
x<-makeCacheMatrix(matrix(1:4,2,2))
debug(cacheSolve)
cacheSolve(x)
